{
  "name": "cca-f-scenario-6-structured-data-extraction",
  "version": "1.0.0",
  "description": "CCA-F Scenario 6 reference implementation: structured extraction with a schema invariant that catches self-contradictory 'confident but null' results.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "extract": "tsx src/cli.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4",
    "@types/node": "^22.9.0"
  }
}
